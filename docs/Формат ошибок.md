# Формат ошибок API

## Краткое описание
Этот документ описывает единый формат ошибок, который возвращает Backend. Он нужен, чтобы фронтенд и бэкенд одинаково понимали, что именно пошло не так, и чтобы ошибки было удобно типизировать через OpenAPI.

Документ отвечает на вопросы:
- как выглядит тело ответа при ошибке;
- какие коды ошибок использовать;
- какие HTTP-статусы возвращать в типовых сценариях (`/token`, `/session`).

---

## Принципы
1) **HTTP-статус отражает класс ошибки**
- `400` — запрос неправильный (не хватает данных, неверный формат).
- `401` — пользователь не авторизован (сессии нет, токен невалиден/просрочен).
- `500` — ошибка на стороне сервера.

2) **`message` — это машинный код ошибки**
- Это не текст для пользователя.
- Код должен быть стабильным, чтобы фронтенд мог писать обработку по нему.

3) **Один формат для всех ошибок**
- Фронтенд всегда ожидает одинаковые поля: `status`, `message`, `timestamp`.

---

## Схема ответа при ошибке
Backend возвращает JSON следующего вида:
- `status`: всегда строка `"error"`.
- `message`: код ошибки (строка из списка ниже).
- `timestamp`: время ошибки в ISO 8601 (UTC).

Пример:
```json
{
  "status": "error",
  "message": "SESSION_EXPIRED",
  "timestamp": "2025-12-20T12:43:28Z"
}
```

---

## Коды ошибок
### Валидация
- `VALIDATION_ERROR` — запрос некорректен (нет token, пустая строка, неверные типы, сломанный JSON).

### OAuth / токен
- `OAUTH_STATE_INVALID` — неверный `state` (защита от подмены).
- `OAUTH_CODE_INVALID` — токен не прошёл проверку (невалиден, просрочен, не парсится по правилам).

### Сессия
- `SESSION_MISSING` — сессии нет (токен/куки не переданы).
- `SESSION_EXPIRED` — сессия есть, но больше не действительна.

### Сервер
- `INTERNAL_ERROR` — непредвиденная ошибка сервера или внешнего сервиса.

> Примечание: если позже понадобится более детальная диагностика, можно добавлять новые `message`, не меняя формат ответа.

---

## Соответствие HTTP-статусов и ошибок
### 400 Bad Request
Когда запрос **некорректный по форме**:
- отсутствуют обязательные поля;
- неверный формат заголовков/тела запроса;
- token пустой/не строка/сломанный JSON.

Обычно:
- `message: VALIDATION_ERROR`

### 401 Unauthorized
Когда запрос корректный, но **авторизация не прошла**:
- токен невалиден или просрочен;
- сессия отсутствует/истекла (если используем /session).

Обычно:
- `OAUTH_CODE_INVALID`
- `SESSION_MISSING`
- `SESSION_EXPIRED`

### 500 Internal Server Error
Когда проблема на стороне сервера/интеграций:
- база данных недоступна;
- исключение в коде;
- упал внешний сервис.

Обычно:
- `INTERNAL_ERROR`

---

## Чек-лист ответов для POST /token
### Успех
- Токен валиден → пользователь создан/обновлён → вернуть `user`
  - `200 OK`

### 400 Bad Request
- `token` отсутствует / пустой → `VALIDATION_ERROR`
- `token` есть, но не строка/JSON сломан → `VALIDATION_ERROR`
- `state` не прошёл проверку (если используется) → `OAUTH_STATE_INVALID`

### 401 Unauthorized
- `token` парсится, но невалиден/просрочен → `OAUTH_CODE_INVALID`

### 500 Internal Server Error
- Ошибка БД/внешнего сервиса/исключение → `INTERNAL_ERROR`

---

## Чек-лист ответов для GET /session
Сессия передаётся через заголовок `Authorization: Bearer <session_token>`.
### Успех
- Токен валиден → сессия активна → вернуть `{ authenticated: true, user }`
  - `200 OK`

### 401 Unauthorized
- Токен/сессия не переданы → `SESSION_MISSING`
- Токен передан, но сессия истекла (если вы различаете expiry) → `SESSION_EXPIRED`
- Токен передан, но невалиден → `OAUTH_CODE_INVALID`

### 400 Bad Request
- Запрос сломан по форме (например, `Authorization` не `Bearer <session_token>`) → `VALIDATION_ERROR`

### 500 Internal Server Error
- Ошибка БД/внешнего сервиса/исключение → `INTERNAL_ERROR`

---

## Чек-лист ответов для POST /session, POST /session/refresh, DELETE /session

### POST /session (создание)
- Успех → `200 OK`
- `token` отсутствует/пустой/не строка → `400 VALIDATION_ERROR`
- `token` невалиден/просрочен → `401 OAUTH_CODE_INVALID`
- Ошибка БД/исключение → `500 INTERNAL_ERROR`

### POST /session/refresh (продление)
- Сессия не передана → `401 SESSION_MISSING`
- Сессия истекла/отозвана → `401 SESSION_EXPIRED`
- Успех → `200 OK`
- Ошибка БД/исключение → `500 INTERNAL_ERROR`

### DELETE /session (logout)
- Сессия не передана → `401 SESSION_MISSING`
- Успех → `200 OK`
- Ошибка БД/исключение → `500 INTERNAL_ERROR`

---

## Таблица ошибок

### Задача: `POST /token` — валидация токена + upsert пользователя
| Ветка логики | HTTP статус | `message` |
|---|---:|---|
| Токен валиден → пользователь создан/обновлён → вернуть `user` | 200 | — |
| `token` отсутствует / пустой | 400 | `VALIDATION_ERROR` |
| `token` есть, но **не парсится** (неверный формат) | 400 | `VALIDATION_ERROR` |
| `state` не прошёл проверку (если используется в этом эндпойнте) | 400 | `OAUTH_STATE_INVALID` |
| `token` парсится, но **невалиден** (подпись/срок/проверка) | 401 | `OAUTH_CODE_INVALID` |
| Ошибка БД/внешнего сервиса/исключение в коде | 500 | `INTERNAL_ERROR` |

### Задача: `GET /session` — проверка сессии и возврат текущего пользователя
| Ветка логики | HTTP статус | `message` |
|---|---:|---|
| Токен валиден → сессия активна → вернуть `{ authenticated: true, user }` | 200 | — |
| Токен/сессия **не переданы** (нет `Authorization`/cookie) | 401 | `SESSION_MISSING` |
| Токен передан, но сессия **истекла** (если вы различаете expiry) | 401 | `SESSION_EXPIRED` |
| Токен передан, но **невалиден** (проверка не прошла) | 401 | `OAUTH_CODE_INVALID` |
| Заголовок/параметры сломаны по форме (например, `Authorization` не `Bearer <token>`) | 400 | `VALIDATION_ERROR` |
| Ошибка БД/внешнего сервиса/исключение в коде | 500 | `INTERNAL_ERROR` |
